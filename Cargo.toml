[workspace]
members = [
    "blockchain-source",
    "core",
    "deps",
    "fraos",
    "indexed-log-map",
    "multiverse",
    "multiverse-source"
]

[workspace.metadata.release]
shared-version = false
tag = true
