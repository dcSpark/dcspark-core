[workspace]
members = [
    "blockchain-source",
    "core",
    "deps",
    "fraos",
    "indexed-log-map",
    "multiverse",
    "utxo-selection",
]

[workspace.metadata.release]
shared-version = false
tag = true
