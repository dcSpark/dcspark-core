[workspace]
members = [
    "blockchain-source",
    "core",
    "deps",
    "fraos",
    "indexed-log-map",
    "multiverse",
    "tools/first-address-from-root",
]

[workspace.metadata.release]
shared-version = false
tag = true
